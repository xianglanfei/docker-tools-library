# Dockerfile for a Node.js build environment

# Start from the official Node.js image, version 20, based on Alpine for smaller size.
# This image comes with Node.js and the npm package manager pre-installed.
FROM node:20-alpine

# Set the default working directory inside the container.
# Any subsequent commands (like 'npm install') will run in this folder.
WORKDIR /app